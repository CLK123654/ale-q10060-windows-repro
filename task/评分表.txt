检查项：可执行测试与交付结构
标准答案：output/playwright.config.mjs和output/tests/creator_review.spec.mjs可由Playwright1.62.1执行；output/reports/release_review.csv可解析；复核表列出的6份回执和6张截图均存在。
关键字段：playwright.config.mjs、creator_review.spec.mjs、release_review.csv的receipt_file和screenshot_file
容差：函数拆分、变量名、源码注释和测试标题可以不同；交付相对路径固定。
失败边界：配置或测试源码缺失，测试不能启动Chromium，复核表不可解析，或表内路径悬空。
可观察证据：Playwright退出码、测试源码、复核表和output成员
客观硬性标准：Chromium测试可运行，复核表有6个唯一case_id，12条证据路径全部命中实际文件。
阈值：三个条件全部成立。
优先级：Gate项
所属层级：L1任务指令遵循层

检查项：登录状态隔离
标准答案：六个case分别通过browser.newContext加载场景指定的storageState。creator_verified、creator_limited和reviewer_readonly都被使用，页面身份提示与storage_state_matrix.csv逐项一致。
关键字段：review_scenarios.csv的storage_state，storage_state_matrix.csv的expected_banner，release_review.csv的expected_banner
容差：case执行顺序不计分，按case_id比较。
失败边界：跨case复用同一上下文，忽略state文件，角色或账号状态与矩阵冲突，或修改输入state绕过检查。
可观察证据：newContext参数、三份state文件、页面note文本和复核表
客观硬性标准：每个case使用独立上下文，三份状态均被覆盖，六条身份提示全部匹配。
阈值：三个条件全部成立。
优先级：高权重项
所属层级：L8运行验证或可执行行为层

检查项：六类业务状态
标准答案：C01为ready_to_submit且submit_enabled=true；C02至C06依次为needs_disclosure、account_limited、schedule_too_soon、needs_alt_text和reviewer_readonly，提交按钮均不可用，status_text与场景输入一致。
关键字段：release_review.csv的case_id、review_state、submit_enabled、status_text
容差：行序不计分，布尔值只接受true或false。
失败边界：漏测案例，状态或提示不符，按钮状态与review_state冲突，或没有通过页面操作触发规则。
可观察证据：复核表、review_scenarios.csv、页面review-state、status消息和按钮断言
客观硬性标准：六个case逐项匹配，只有C01允许提交。
阈值：所有指定状态、提示与按钮状态均正确。
优先级：高权重项
所属层级：L6领域计算与专业规则层

检查项：下载回执
标准答案：六个case都通过download事件保存页面给出的suggestedFilename。回执列顺序与review_policy.json一致，C01至C05各有1行数据，C06有3行数据，首行review_state与复核表一致。
关键字段：receipt_file、receipt_rows、downloadable_columns、review_state
容差：CSV换行可为LF或CRLF，记录内容不设容差。
失败边界：未捕获真实download事件，文件名被替换，回执缺失，列顺序错误，或C06行数不是3。
可观察证据：download对象、6份回执、复核表和页面下载文件名
客观硬性标准：六份文件名、列顺序、行数和首行状态逐项正确。
阈值：四类检查全部通过。
优先级：高权重项
所属层级：L9跨产物一致性与可追溯层

检查项：可访问名称与截图
标准答案：每个case都能通过role或label定位素材核验表单、核验结果、status消息及核验素材、提交审核、下载当前核验回执三个按钮，并保存对应的完整页面PNG。
关键字段：status_role、form_name、result_region、named_buttons、screenshot_file
容差：PNG字节和页面渲染细节可随系统字体变化，页面内容和尺寸必须有效。
失败边界：使用脆弱的坐标操作，任一命名控件无法从可访问树定位，多个case共用截图，或截图文件无效。
可观察证据：Playwright定位器、ariaSnapshot、复核表和6张PNG
客观硬性标准：六行可访问字段一致，三个按钮名称齐全，六张截图均为有效PNG。
阈值：三类检查全部通过。
优先级：普通项
所属层级：L7可访问性与交互语义层

检查项：本地运行与失败收口
标准答案：浏览器请求只指向127.0.0.1，同一输入重复执行保持业务字段和回执内容稳定。必要的场景、规则或state文件缺失时进程非零退出，本轮reports、receipts和screenshots均不存在。
关键字段：webServer、请求主机、进程退出码和三个动态目录
容差：测试日志和截图字节可不同，业务字段与回执内容必须一致。
失败边界：访问外部后端，异常返回0，沿用旧动态文件，修改输入，或Windows运行需要Linux环境。
可观察证据：请求记录、两次运行结果、失败回放和输入哈希
客观硬性标准：本地请求、重复业务结果和失败关闭三项全部成立。
阈值：三个条件全部通过。
优先级：Gate项
所属层级：L10工程复现与故障收口层

