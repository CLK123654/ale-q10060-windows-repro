读取验收案例、页面规则和登录状态矩阵，核对每个state_file都能定位到实际JSON。
配置Playwright的Chromium项目、本地页面服务、下载接收、时区和串行运行边界。
按case创建独立浏览器上下文并加载指定storageState，确认页面身份提示符合矩阵。
使用可访问定位器填写素材表单，触发核验后读取业务状态、提示文字和提交按钮状态。
监听download事件并保存页面生成的回执，核对文件名、列顺序、数据行数和首行状态。
检查表单、结果区域、状态消息和三个按钮的可访问角色与名称。
为每个case保存核验后的完整页面截图，并把回执和截图路径写入发布复核表。
执行npm run test:e2e，确认只访问本地服务，失败时清理本轮动态交付。

